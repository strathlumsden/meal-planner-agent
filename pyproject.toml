[project]
name = "meal-planner-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Strath Lumsden", email = "strath.lumsden@outlook.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
meal-planner-agent = "meal_planner_agent:main"

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.22",
]
